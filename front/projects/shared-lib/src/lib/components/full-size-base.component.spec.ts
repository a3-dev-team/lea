import { FullSizeBaseComponent } from './full-size-base.component';

describe('FullSizeBaseComponent', () => {
  it('should create an instance', () => {
    const directive = new FullSizeBaseComponent();
    expect(directive).toBeTruthy();
  });
});
