name: Angular - Build & Test

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  build_test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Use Node.js 16.x
        uses: actions/setup-node@v1
        with:
          node-version: 16.x

      - name: npm ci, build & test
        working-directory: front
        run: |
          npm ci
<<<<<<< HEAD:.github/workflows/angular-build-and-test.yml
          npm run build:ci 
          npm run test:ci
=======
          npm run build:ci --if-present
          npm run test:ci --if-present
>>>>>>> 904ea47f976ce12353bc405375ef47354dc03843:.github/workflows/actions-check-build-and-test.yml
