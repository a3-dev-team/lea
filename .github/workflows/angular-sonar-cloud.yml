name: Angular - SonarCloud

on:
  workflow_dispatch:

  push:
    branches:
      - dev

jobs:
  build_test_sonar-scanner:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          # Disabling shallow clone is recommended for improving relevancy of reporting
          fetch-depth: 0

      - name: Use Node.js 16.x
        uses: actions/setup-node@v1
        with:
          node-version: 16.x

      - name: npm ci, build & test
        working-directory: front
        run: |
          npm ci
<<<<<<< HEAD:.github/workflows/angular-sonar-cloud.yml
          npm run build:ci 
          npm run test:ci:code-coverage
=======
          npm run build:ci --if-present
          npm run test:ci:code-coverage --if-present
>>>>>>> 904ea47f976ce12353bc405375ef47354dc03843:.github/workflows/actions-send-to-sonar.yml

      - name: SonarCloud Scan
        uses: sonarsource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
